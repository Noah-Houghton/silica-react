import { RendererProps } from '@jsonforms/core';
import { VanillaRendererProps, WithChildren } from '../index';
export declare const SilicaJsonFormsLayout: ({ className, children, visible }: RendererProps & VanillaRendererProps & WithChildren) => JSX.Element;
